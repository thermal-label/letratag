# Interface: RequestPrinterOptions

## Properties

| Property | Type | Description |
| ------ | ------ | ------ |
| <a id="property-nameprefix"></a> `namePrefix?` | `string` | Override the device-name filter passed to `navigator.bluetooth.requestDevice`. Falls back to the registry's `namePrefix` when omitted. |

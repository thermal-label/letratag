# Variable: LT\_PAPER\_WHITE

```ts
const LT_PAPER_WHITE: LetraTagMedia;
```

Canonical default — the white-paper cassette ships in the box with
every LT-200B. Single named const, not a per-cassette export.

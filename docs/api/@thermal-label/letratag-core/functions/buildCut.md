# Function: buildCut()

```ts
function buildCut(command: number): Uint8Array;
```

`[0x1B, 0x70, command]` — cut directive.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `command` | `number` |

## Returns

`Uint8Array`

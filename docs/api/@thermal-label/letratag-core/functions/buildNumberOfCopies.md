# Function: buildNumberOfCopies()

```ts
function buildNumberOfCopies(copies: number): Uint8Array;
```

`[0x1B, 0x23, copies]` — number of copies for this job.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `copies` | `number` |

## Returns

`Uint8Array`
